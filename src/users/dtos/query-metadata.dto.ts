import { QueryMetadataDto as BaseQueryMetadataDto } from 'src/utils/dtos/query-metadata.dto';

// Extend the base query metadata DTO with any user-specific query parameters if needed
class QueryMetadataDto extends BaseQueryMetadataDto {
  // Add any user-specific query parameters here
}

export default QueryMetadataDto;
